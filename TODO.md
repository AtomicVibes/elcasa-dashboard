- [ ] Understand existing Leave Requests page implementation
- [ ] Implement filter dropdown (Status + Sort by Date) using Shadcn UI Popover/Dropdown Menu
- [ ] Add local component state for status filter + date sort
- [ ] Compute filtered + sorted requests via client-side `.filter()` and `.sort()`
- [ ] Update table rendering to use filtered/sorted array
- [ ] Apply styling: dark dropdown background + selection/hover highlight #FFC107 with black text
- [ ] Ensure "All Statuses" resets status filter while keeping chosen date sorting
- [ ] Sanity check TS types and table empty state

